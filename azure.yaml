# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: chainlit-openai-assistantsapi-aca-starter
metadata:
    template: azd-init@1.11.1
services:
    chainlit:
        project: src
        host: containerapp
        language: python
        docker:
            path: dockerfile
